<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product List</title>
    <style>
        /* General Page Styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #F9F9F9;
            margin: 0;
            padding: 20px;
            text-align: center;
        }

        /* Main Container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Heading Styles */
        h1 {
            font-size: 28px;
            color: #333;
            margin-bottom: 20px;
        }

        .category-title {
            font-size: 22px;
            color: #555;
            margin-top: 30px;
            text-transform: uppercase;
            text-align: left;
            margin-left: 20px;
        }

        /* Product Row Layout */
        .product-row {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: flex-start;
            padding: 10px 20px;
        }

        /* Product Card Styling */
        .product-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            width: 180px;
            /* Smaller width for rows */
            text-align: center;
        }

        .product-card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        /* Product Image */
        .product-card img {
            width: 100%;
            height: 120px;
            /* Smaller image */
            object-fit: cover;
        }

        /* Product Info */
        .product-info {
            padding: 10px;
        }

        .product-info strong {
            font-size: 16px;
            color: #222;
            display: block;
            margin-bottom: 5px;
        }

        .product-info p {
            font-size: 14px;
            color: #777;
        }

        /* Link Styling */
        .product-card a {
            text-decoration: none;
            color: inherit;
            display: block;
        }
    </style>
</head>

<body>

    <div class="container">
        <h1>Product List</h1>

        <% for (const category in categorizedProducts) { %>
            <h2 class="category-title">
                <%= category %>
            </h2>

            <div class="product-row">
                <% categorizedProducts[category].forEach((product)=> { %>
                    <div class="product-card">
                        <a href="/make-yours/<%= product.id %>">
                            <img src="/<%= product.id %>.png" alt="<%= product.name %>">
                            <div class="product-info">
                                <strong>
                                    <%= product.name %>
                                </strong>
                                <p>
                                    <%= product.base_price %> THB
                                </p>
                            </div>
                        </a>
                    </div>
                    <% }); %>
            </div>
            <% } %>
    </div>

</body>

</html>