<style>
    .container {
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        padding: 20px;
        box-sizing: border-box;
    }
</style>

<div class="container mt-15">
    <h1>My Account</h1>
    <h3>หากไม่ได้รับสินค้า สามารถแจ้งได้ที่ Line: @celestar</h3>
    
    <div class="bar">
        <a href="/myorders">ข้อมูลผู้ใช้</a>
        <a href="/myorders?page=pending">รอดำเนินการ</a>
        <a href="/myorders?page=check">รอตรวจสอบ</a>
        <a href="/myorders?page=accept">ที่ต้องชำระเงิน</a>
        <a href="/myorders?page=decline">ออเดอร์ถูกปฏิเสธ</a>
        <a href="/myorders?page=paid">รับออเดอร์แล้ว</a>
        <a href="/myorders?page=success">จัดส่งสำเร็จ</a>
    </div>
    
    
    <div class="content">
        <p>Hello <strong><%= user.email.split('@')[0]%></strong> (not <strong><%= user.email.split('@')[0]%></strong>? <a href="/logout">Log out</a>)</p>
        <% if (page === '') {%>
            <%- include('editAccount') %>
        <% } else { %>
            <%- include('orders') %>
        <% } %>
    </div>
    
</div>

