<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customized by You!</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <script src="/js_cus_ejs/customer.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,100..900;1,9..144,100..900&family=Noto+Sans+Thai:wght@100..900&display=swap');
        
        body {
            background-color: #F0E9E9;
            margin: 0 auto;
            font-family: "Noto Sans Thai", sans-serif;
        }

        .div1 {
            display: flex;
            justify-content: center;
            gap: 10px;
            flex-wrap: wrap;
            padding: 10px;
            font-family: "Noto Sans Thai", sans-serif;
        }

        .item.prod {
            width: 80vh;
            height: 75vh;
            overflow: hidden;
        }

        .item.custom {
            width: 80vh;
            padding: 30px;
            background-color: #BF8579;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            border-radius: 10px;
        }

        .item img {
            width: 100%;
            border-radius: 10px;
        }

        .heading-div {
            font-family: "Noto Sans Thai";
            text-align: center;
            margin: 0;
        }

        .custom.form,
        .custom.name {
            margin: 0;
        }

        label,
        input,
        select,
        button {
            margin-bottom: 7px;
            padding-bottom: 8px;
            padding-right: 10px;
        }

        textarea,
        input,
        select,
        button {
            background-color: #F2E9E4;
            border-radius: 10px;
            color: #A56037;
            padding: 8px;
            border: 1px solid #ffffff;
            height: 50px;
            /* text-align: center; */
        }

        input[type="radio"] {
            height: 20px;
        }

        h1,
        h2,
        p,
        label {
            color: #fff;
        }

        select,
        input[type="text"] {
            margin-bottom: 20px;
        }

        .engraveChoice{
            width: 30px;
        }
        .heading-div {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            margin: 0 50px;
        }

        button {
            background-color: black;
            color: aliceblue;
            padding: 10px;
            cursor: pointer;
        }

        .submit {
            background-color: #450100;
            color: white;
            font-size: 16px;
            font-weight: bold;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
            width: 100%;
            text-transform: uppercase;
        }

        .submit:hover {
            background-color: #F0E9E9;
            color: #A56037;
            transform: scale(1.05);
        }

        .submit:active {
            background-color: #555;
            transform: scale(0.98);
        }


        .submit {
            width: 100%;
        }

        @media screen and (max-width: 768px) {
            .div1 {
                flex-direction: column;
                align-items: center;
            }

            .item.custom {
                max-width: 90vw;
            }

            .heading-div {
                flex-direction: column;
                align-items: center;
                padding: 10px;
            }
        }

        @media screen and (max-width: 480px) {
            .heading-div h1 {
                font-size: 18px;
            }

            select,
            input,
            button {
                font-size: 14px;
                padding: 8px;
                border-radius: 8px;
            }
        }

        .back {
            text-decoration: none;
            color: #A56037;
            font-weight: bold;
            padding: 8px 12px;
            border-radius: 5px;
            transition: color 0.3s, background-color 0.3s;
        }

        .back:hover {
            color: white;
            /* Change text color on hover */
            background-color: #BF8579;
            /* Add background */
            text-decoration: none;
            /* Ensure no underline appears */
        }

        textarea {
            margin: 10px 0;
            width: 100%;
            height: 100px;
        }

        .tooltip-container {
            position: relative;
            display: inline-block;
            cursor: pointer;
        }

        .tooltip {
            visibility: hidden;
            background-color: rgba(0, 0, 0, 0.75);
            color: #fff;
            text-align: left;
            padding: 10px;
            border-radius: 5px;
            position: absolute;
            z-index: 100;
            width: 200px;
            bottom: 125%;
            left: 50%;
            opacity: 0;
            transition: opacity 0.3s ease-in-out;
        }

        .tooltip-container:hover .tooltip {
            visibility: visible;
            opacity: 1;
        }

        @font-face {
            font-family: IBMPlexSansThai;
            src: url(./IBMPlexSansThai-Regular.ttf);
        }

        body {
            font-family: IBMPlexSansThai;
            overflow-x: hidden;
            width: 100vw;
            user-select: none;
        }

        :root {
            --font-basetext: "Noto Sans Thai";
            --font-weight-basetext: 400;
            --font-style-basetext: normal;
            --font-secondary: 'Noto Sans Thai';
            --font-weight-secondary: 400;
            --font-style-secondary: normal;
            --font-logo: "Fraunces";
            --font-weight-logo: 600;
            --font-style-logo: normal;
            --font-thai: "Noto Sans Thai";
            --font-weight-thai: 400;
            --font-style-thai: normal;
            --color-red: #580201;
            --color-veryred: #450100;
            --color-bs: #A56037;
            --color-bg: #F0E9E9;
            --color-bar: #F2E9E4;
            --color-white: #FFFFFF;
            --color-pink: #BF8579;
            --color-softpink: #EDD1CC;
        }

        html {
            scroll-behavior: smooth;
        }

        .font-basetext {
            font-family: var(--font-basetext);
            font-weight: var(--font-weight-basetext);
            font-style: var(--font-style-basetext);
        }

        .font-secondary {
            font-family: var(--font-secondary);
            font-weight: var(--font-weight-secondary);
            font-style: var(--font-style-secondary);
        }

        .font-logo {
            font-family: var(--font-logo);
            font-weight: var(--font-weight-logo);
            font-style: var(--font-style-logo);
        }


        .pop-up {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 100%;
            max-width: 50%;
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            z-index: 1000;
            display: none;
            justify-content: center;
            
        }

        .pop-up-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            z-index: 999;
            
        }

        .pop-head {
            text-align: center;
            font-weight: bold;
        }

        .order {
            max-height: 300px;
            overflow-y: auto;
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px 0;
            background: #f9f9f9;
            border-radius: 5px;
            width: 100%;
        }

        .order-detail {
            margin: 12px;
        }

        .but-pop {
            display: flex;
            justify-content: center;
            bottom: 0;
        }
        .but-pop button {
            margin: 10px;
            padding: 10px 20px;
            background-color: #450100;
            border: 1px solid #450100;
            border: 2px solid transparent;
            border-radius: 30px;
            color: white;   
            cursor: pointer;
            font-size: 14px;
            font-weight: 700;
            
        }

        .but-pop button:hover {
            background-color: white;
            border: 1px solid #450100;
            color: #450100;
        }

        .order p {
            color: #450100;
        }

        .checkout {
            margin-top: 30px;
            text-align: right;
        }

        .checkout p {
            font-size: 25px;
        }
    </style>

</head>

<body>
    <%- include('topbar') %>
    <!-- <header class="fixed z-50 bar-custom shadow-xl shadow-gray-600/10 w-full px-10 py-3 flex items-center gap-x-4">
        <h1 class="font-logo text-3xl" style="color: var(--color-red);">Celestar</h1>

        <nav class="ml-auto">
            <ul class="flex space-x-6 font-secondary text-[var(--color-red)]">
                <li><a href="Home-Custumer.html" class="hover:underline">หน้าแรก</a></li>
                <li><a href="#" class="hover:underline">เกี่ยวกับ</a></li>
                <li><a href="Home-Custumer.html#Contact" class="hover:underline">ติดต่อ</a></li>
                <li><a href="#" class="hover:underline text-[var(--color-bs)]">ชื่อบัญชีผู้ใช้</a></li>
            </ul>
        </nav>
    </header> -->
    <div class="top-bar" style="height: 100px;"></div>

    <div class="heading-div">
        <a class="back" href="/product">ย้อนกลับ</a>
        <h1 style="color: #580201;font-size: 30px;padding-right: 30px;">ปรับแต่ง<%= product.category %>ของคุณ</h1>
    </div>

    <div class="div1">
        <!-- Image Section -->
        <div class="item prod">
            <img src="/<%= product.image_path %>" alt="Product Image">
        </div>

        <!-- Customization Section -->
        <div class="item custom">
            <div class="custom name">
                <p style="margin-bottom: 0;" id="category"><%= product.category %></p>
                <h2 style="margin-top: 0; font-size: 40px;font-family: Fraunces;">
                    <%= product.name %>
                </h2>
                <p>
                    <%= product.description %>
                </p>
                <p><strong>ราคา : <%= product.base_price %> THB</strong></p>
            </div>

        <!-- Form Section -->
        <div class="custom form">
            <form id="customizationForm">
                <%if (product.category==='แหวน') {%>
                    <strong><label for="size">ขนาด :</label></strong>
                    <input type="number" name="size" id="size" min="16" style="width: 50px;text-align: center;">
                     <br>
                <%};%>

                <strong><label for="material">วัสดุของ<%= product.category %> : </label></strong> <br>
                <select name="material" id="material" onchange="toggleEngraveOption()">
                    <% materialsData.forEach((item)=> { %>
                        <option value="<%= item.name %>-<%= item.price %>">
                            <%= item.name %>: <%= item.price %> บาท
                        </option>
                    <% }); %>
                </select>
                <br>

                <!-- ตัวเลือกสำหรับการสลักชื่อ -->
                <div>
                    <strong><label for="color">สีของ<%= product.category%> : </label></strong> <br>
                    <input type="color" id="pickedcolor" name="pickedcolor" value="<%= product.color %>"
                        style="height: 50px; padding: 0;">
                    <br>

                    <strong><label>เพิ่มข้อความบน<%= product.category%>ของคุณ : </label></strong><br>
                    <label style="display: inline-flex; align-items: center; gap: 5px;">
                        <input class="engraveChoice" class="engraveChoice" type="radio" name="engraveChoice" class="engraveChoice" value="yes"
                            onclick="toggleEngraveMaterialText(true)" style="margin: 0;">
                            สลัก
                    </label>

                    <label style="display: inline-flex; align-items: center; gap: 5px;">
                        <input class="engraveChoice" class="engraveChoice" type="radio" name="engraveChoice" class="engraveChoice" value="none"
                                onclick="toggleEngraveMaterialText(false)" checked style="margin: 0;"> ไม่สลัก
                    </label>
                </div>

                <!-- ช่องพิมพ์ข้อความสำหรับการสลัก -->
                <div id="engraveMaterialTextContainer" style="display: none;">
                    <strong><label for="engraveMaterialText">สลักลงบน<%= product.name %>ของคุณ</label></strong>
                    <input type="text" name="engraveMaterialText" id="engraveMaterialText"
                        placeholder="ข้อความสลักของคุณ" maxlength="6">
                </div>

                <strong><label for="stone">อัญมณี : </label></strong>
                <select name="stone" id="stone">
                    <% let stoneCount=0; %>
                        <% stonesData.forEach((item)=> { %>
                            <option value="<%= item.name %>-<%= item.price %>">
                                <%= item.name %>: <%= item.price %> บาท/ชิ้น
                            </option>
                        <% stoneCount++; %>
                    <% }); %>
                </select><br>

                <% if (product.category==='สร้อยข้อมือ' || product.category==='สร้อยคอ' ) { %>
                    <% if (pendantsData.length> 0) { %>
                        <strong><label>จี้ : </label></strong><br>
                            <% pendantsData.forEach((pendant, index)=> { %>
                                <div class="pendant-selection">
                                    <label class="tooltip-container">
                                        <label style="display: inline-flex; align-items: center;">
                                            <input type="checkbox" id="pendant-<%= index %>" name="pendent"
                                                value="<%= pendant.name %>-<%= pendant.price %>" onclick="toggleInput('pendant-<%= index %>-num')"
                                                style="margin: 0;">
                                            <strong><%= pendant.name %></strong>
                                        </label>

                                        <span class="tooltip">
                                            <strong>ราคา:</strong>
                                            <%= pendant.price %> บาท<br>
                                            <strong>รายละเอียด:</strong>
                                            <%= pendant.description %>
                                        </span>
                                    </label>
                                    <div id="pendant-<%= index %>-num" style="display: none;">
                                        <strong><label for="<%= pendant.name %>-num">จำนวน:</label></strong>
                                        <input type="number" name="<%= pendant.name %>-num" value="1" min="1" max="10"><br>
                                        <strong><label for="<%= pendant.name %>-color">Color of pendant:</label></strong>
                                        <input type="color" value="#E5E4E2" name="<%= pendant.name %>-color"id="<%= pendant.name %>-color">
                                        <br>
                                        <strong><label>เพิ่มข้อความสลักบนจี้ของคุณ</label></strong><br>
                                        <label>
                                            <input class="engraveChoice" class="engraveChoice" type="radio" name="<%= pendant.name %>-engrave" value="yes"
                                                onclick="toggleEngravePendantText('<%= pendant.name %>', true)">
                                                สลัก
                                        </label>
                                        <label>
                                            <input class="engraveChoice" class="engraveChoice" type="radio" name="<%= pendant.name %>-engrave" value="none"
                                                onclick="toggleEngravePendantText('<%= pendant.name %>', false)" checked> ไม่สลัก
                                        </label>
                                        <!-- ช่องกรอกข้อความสำหรับการสลัก -->
                                        <div id="engravePendantTextContainer-<%= pendant.name %>" style="display: none;">
                                            <strong><label for="engravePendantText-<%= pendant.name %>">เพิ่มข้อความสลักบนจี้ของคุณ</label></strong>
                                            <input type="text" maxlength="6" name="engravePendantText-<%= pendant.name %>" id="engravePendantText-<%= pendant.name %>"
                                            placeholder="ข้อความสลักของคุณ">
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                    <% } else { %>
                        <p>No pendants available for this bracelet.</p>
                <% } %>
                <% } %>


                <div class="add-image">
                    <strong><label>รูปภาพอ้างอิง : </label></strong> <br>
                    <input type="file" name="refImage">
                </div>
                <div class="add-description">
                    <strong><label>รายละเอียดเพิ่มเติม : </label></strong>
                    <textarea name="description" id="description" placeholder="ระบุข้อความถึงร้านค้าได้เลย!"></textarea>
                </div>
                <div class="checkout">
                    <!-- <p id="total">ราคารวม: 0 บาท</p> -->
                    <button class="submit" type="submit" onclick="check_customize(event)">สั่งเลย!</button>
                </div>
            </form>
        </div>
    </div>
</div>
<div class="pop-up-overlay" id="pop-up-overlay"></div>
<div class="pop-up" id="pop-up">
    <div class="pop-content">
        <div class="pop-head">
            <%if (user.isMember) {%>
                <h2 style="color: rgba(60, 42, 30, 1);">ออเดอร์ของคุณ <%=user.email.split('@')[0]%></h2>
            <%}else{%>
                <h2 style="color: rgba(60, 42, 30, 1);">ออเดอร์ของคุณ</h2>
            <%}%>
            <h3 id="total-pop">TOTAL: 0 BAHT</h3>
        </div>

        <div class="order" id="order">

        </div>
        <div class="but-pop">
            <%if (user.isMember) {%>
                <button onclick="confirm()">CONFIRM</button>
            <%}%>
            <button onclick="closePopup()">CANCEL</button>
        </div>
        
    </div>
</div>


</body>

</html>