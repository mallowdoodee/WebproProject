<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Celestar - Jewelry</title>
    <link href="public/css/Home-Customer.css" rel="stylesheet">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+Thai:wght@100..900&display=swap" rel="stylesheet">
    <style>
        /* Include the styles from your current code */
        body {
            font-family: IBMPlexSansThai;
            overflow-x: hidden;
            width: 100vw;
            user-select: none;
            background-color: #F9F9F9;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        /* Additional CSS Styles (like product-card and etc.) */
    </style>
</head>

<body>
    <div class="container">
        <!-- Include Topbar -->
        <%- include('topbar') %>

        <article class="bg-[var(--color-bar)] mt-15 flex font-thai">
            <!-- Include Sidebar -->
            <%- include('sidebar') %>

            <!-- Include Products -->
            <%- include(body) %>
        </article>
    </div>
</body>
</html>
